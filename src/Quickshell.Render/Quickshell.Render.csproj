<Project Sdk="Microsoft.NET.Sdk">

  <!-- The D3D11 code. It may read what the terminal holds; it may never see a network
       type, so Quickshell.Transport is deliberately absent from the references below. -->
  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <RootNamespace>Quickshell.Render</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Vortice.Direct3D11" Version="3.8.3" />
    <PackageReference Include="Vortice.DXGI" Version="3.8.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Quickshell.Terminal\Quickshell.Terminal.csproj" />
  </ItemGroup>

</Project>
