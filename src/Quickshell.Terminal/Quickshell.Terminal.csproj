<Project Sdk="Microsoft.NET.Sdk">

  <!-- The buffer and the parser. This project references nothing graphical and nothing
       from the network, which is why it carries no ProjectReference at all. -->
  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <RootNamespace>Quickshell.Terminal</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- The transition table is internal because nothing outside the parser may index it, and the
         test that proves it complete has to. -->
    <InternalsVisibleTo Include="Quickshell.Terminal.Tests" />
  </ItemGroup>

</Project>
