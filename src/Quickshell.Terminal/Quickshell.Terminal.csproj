<Project Sdk="Microsoft.NET.Sdk">

  <!-- The buffer and the parser. This project references nothing graphical and nothing
       from the network, which is why it carries no ProjectReference at all. -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>Quickshell.Terminal</RootNamespace>
  </PropertyGroup>

</Project>
