# Build output
bin/
obj/

# Visual Studio / Rider per-user state
*.user
*.suo
.vs/
.idea/

# Test and publish artifacts
[Tt]est[Rr]esults/
publish/
artifacts/

# BenchmarkDotNet writes its logs and reports beside the project. They are build output of a run,
# not the committed results file, which the harness writes to benchmarks/results.
BenchmarkDotNet.Artifacts/

# NuGet
*.nupkg
packages/

# The SSH probe's fixture makes a private key and a signing CA on every run. They authorise two
# throwaway containers on loopback and nothing else, and a committed private key is a habit rather
# than an incident.
prototypes/SshProbe/fixture/keys/

# roadkeep engine pin, when one is vendored
.roadkeep/
